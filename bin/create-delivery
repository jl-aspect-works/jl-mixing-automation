#!/usr/bin/env bash
# JL Mixing Automation create-delivery launcher.
set -eu
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/jl-mixing-python-command" jl_mixing.create_delivery_cli "$@"
