#!/usr/bin/env bash
# JL Mixing Automation validate-intake launcher.
set -eu
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/jl-mixing-python-command" jl_mixing.validate_intake_cli "$@"
